<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/ll_root"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="12dp"
        android:paddingBottom="11dp">

        <TextView
            android:id="@+id/tv_is_must"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="*"
            android:textColor="@color/color_main"
            android:textSize="13dp" />

        <TextView
            android:id="@+id/tv_picture_type"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="简易赔案协议书(2/10)"
            android:textColor="@color/text_color_10"
            android:textSize="13dp" />

    </LinearLayout>

    <CheckBox
        android:id="@+id/iv_select_status"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:button="@drawable/camera_picture_type_status_select" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@+id/ll_root"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="15dp"
        android:background="@color/divider" />
</RelativeLayout>